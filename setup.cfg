[tool:pytest]
# pytest config is in pyproject.toml — this file exists for tool compatibility only.

[coverage:run]
source = core,users,tickets
omit =
    */migrations/*
    manage.py
    config/*
    */__pycache__/*

[coverage:report]
show_missing = True
skip_covered = False
